# Waitlist form copy (Tally / Google Form)

**Form title:** Rowproof — hosted reconciliation, early access

**Body copy:**

Rowproof's CLI is free and open source. We're building a hosted service on top of it for teams running migrations or CDC replication at scale:

- Scheduled reconciliation runs — no cron jobs to babysit
- Run history — see when a table pair last matched, and when it didn't
- Slack/email alerts the moment a scheduled run finds a difference
- Sign-off reports for a whole migration, not just one table pair, ready to attach to a cutover approval

$150/month per workspace.

Leave your email and we'll reach out when it's ready.

**Fields:**
- Email (required)
- Company (optional)
- How many table pairs are you reconciling, roughly? (optional — free text or a small range picker: 1–10 / 11–50 / 50+)

**Submit button label:** Join the waitlist
