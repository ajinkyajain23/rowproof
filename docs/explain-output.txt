-- source: row-count and bounds
SELECT MIN("id"), MAX("id"), COUNT(*) FROM "public"."orders"
-- target: row-count and bounds
SELECT MIN(`id`), MAX(`id`), COUNT(*) FROM `orders`
-- source: uniqueness check on the key
SELECT "id", COUNT(*) FROM "public"."orders" GROUP BY "id" HAVING COUNT(*) > 1 LIMIT 1
-- target: uniqueness check on the key
SELECT `id`, COUNT(*) FROM `orders` GROUP BY `id` HAVING COUNT(*) > 1 LIMIT 1
-- source: per-segment count + hash (one per segment)
SELECT COUNT(*), ((sum(((('x' || substr(md5(("id"::text) || E'\x1f' || (CASE WHEN "customer" IS NULL THEN E'\\N' ELSE ("customer"::text) END) || E'\x1f' || (CASE WHEN "amount" IS NULL THEN E'\\N' ELSE to_char(((CASE WHEN ((("amount")::numeric * power(10::numeric, 2)) - floor((("amount")::numeric * power(10::numeric, 2)))) = 0.5 THEN (CASE WHEN floor((("amount")::numeric * power(10::numeric, 2)))::numeric % 2 = 0 THEN floor((("amount")::numeric * power(10::numeric, 2))) ELSE floor((("amount")::numeric * power(10::numeric, 2))) + 1 END) ELSE round((("amount")::numeric * power(10::numeric, 2))) END) / power(10::numeric, 2)), 'FM999999999999999990.00') END) || E'\x1f' || (CASE WHEN "created_at" IS NULL THEN E'\\N' ELSE to_char(timezone('UTC', "created_at"), 'YYYY-MM-DD"T"HH24:MI:SS.US"Z"') END)), 1, 16))::bit(64)::bigint))::numeric) % 18446744073709551616 + 18446744073709551616) % 18446744073709551616) FROM "public"."orders" WHERE "id" >= '<segment lower bound>' AND "id" < '<segment upper bound>'
-- target: per-segment count + hash (one per segment)
SELECT COUNT(*), ((sum(toInt256(reinterpretAsInt64(reverse(unhex(substr(lower(hex(MD5(concat(toString(`id`), char(31), toString(`customer`), char(31), concat((CASE WHEN (CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END) < 0 THEN '-' ELSE '' END), left(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0'), length(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0')) - 2), '.', right(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0'), 2)), char(31), concat(formatDateTime(toDateTime64(`created_at`, 6, 'UTC'), '%Y-%m-%dT%H:%i:%S'), '.', leftPad(toString((toUnixTimestamp64Micro(toDateTime64(`created_at`, 6, 'UTC')) % 1000000)), 6, '0'), 'Z'))))), 1, 16)))))) % toInt256('18446744073709551616') + toInt256('18446744073709551616')) % toInt256('18446744073709551616')) FROM `orders` WHERE `id` >= '<segment lower bound>' AND `id` < '<segment upper bound>'
-- source: row-level fetch for a mismatched segment
SELECT "id", (CASE WHEN "customer" IS NULL THEN E'\\N' ELSE ("customer"::text) END), (CASE WHEN "amount" IS NULL THEN E'\\N' ELSE to_char(((CASE WHEN ((("amount")::numeric * power(10::numeric, 2)) - floor((("amount")::numeric * power(10::numeric, 2)))) = 0.5 THEN (CASE WHEN floor((("amount")::numeric * power(10::numeric, 2)))::numeric % 2 = 0 THEN floor((("amount")::numeric * power(10::numeric, 2))) ELSE floor((("amount")::numeric * power(10::numeric, 2))) + 1 END) ELSE round((("amount")::numeric * power(10::numeric, 2))) END) / power(10::numeric, 2)), 'FM999999999999999990.00') END), (CASE WHEN "created_at" IS NULL THEN E'\\N' ELSE to_char(timezone('UTC', "created_at"), 'YYYY-MM-DD"T"HH24:MI:SS.US"Z"') END) FROM "public"."orders" WHERE "id" >= '<segment lower bound>' AND "id" < '<segment upper bound>' ORDER BY "id"
-- target: row-level fetch for a mismatched segment
SELECT `id`, toString(`customer`), concat((CASE WHEN (CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END) < 0 THEN '-' ELSE '' END), left(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0'), length(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0')) - 2), '.', right(leftPad(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END))), greatest(length(toString(abs((CASE WHEN ((toDecimal256(`amount`, 30) * intExp10(2)) - floor((toDecimal256(`amount`, 30) * intExp10(2)))) = 0.5 THEN (CASE WHEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) % 2 = 0 THEN toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) ELSE toInt256(floor((toDecimal256(`amount`, 30) * intExp10(2)))) + 1 END) ELSE toInt256(round((toDecimal256(`amount`, 30) * intExp10(2)))) END)))), 3), '0'), 2)), concat(formatDateTime(toDateTime64(`created_at`, 6, 'UTC'), '%Y-%m-%dT%H:%i:%S'), '.', leftPad(toString((toUnixTimestamp64Micro(toDateTime64(`created_at`, 6, 'UTC')) % 1000000)), 6, '0'), 'Z') FROM `orders` WHERE `id` >= '<segment lower bound>' AND `id` < '<segment upper bound>' ORDER BY `id`
