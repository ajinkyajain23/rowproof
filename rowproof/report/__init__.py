# JSON/HTML rendering from the DiffResult model.
#
# M0 only needs the terminal + JSON forms, which live in cli/render.py for
# now since there's so little of them. The HTML sign-off report (spec
# §8.3) is M3 scope — this package is the placeholder for it so the module
# layout in spec §3 stays intact from day one.
